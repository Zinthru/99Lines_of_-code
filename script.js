let friends = ["Ty","Brad","Walker","Breelyn","Jip"];

for(i=0;i<friends.length; i++) {
    let friend = frineds[i]; {
        for (t=99;t>0;t--) {
            let negativeOne = t -1;
            if(t>1) {
                console.log(s+" lines of code in the file, " + t + " lines of code; "+friend+" strikes one out, clears it all out, "+negativeOne+" lines of code in the file");
            } else  {
                console.log("1 line of code in the file, 1 line of code; " + friend+ " strikes one out, clears it all out, no more lines of code in the file");
            }
        }
    }
};